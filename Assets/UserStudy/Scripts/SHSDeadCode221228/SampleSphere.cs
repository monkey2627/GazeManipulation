using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UserStudy
{
    /// <summary>
    /// 该脚本存放采样点的相关属性，挂载在采样点物体上
    /// </summary>
    public class SampleSphere : MonoBehaviour
    {

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

